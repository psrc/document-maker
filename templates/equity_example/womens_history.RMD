---
output:
  pdf_document:
    latex_engine: lualatex
    includes:
      in_header: "X:/DSA/Trends/equity/headers/trend_format_equity.tex"
  html_document:
    df_print: paged
fontsize: 12pt
---

\setmainfont{Poppins}

# Celebrating Women's History
```{r setup, include=FALSE}

options(tinytex.verbose=TRUE)
library(psrcplot)
library(psrccensus)
library(psrc.travelsurvey)
library(tidyverse)
library(knitr)
library(magick)
library(openxlsx)
library(htmlwidgets)

elmer_connect <- function(){
  DBI::dbConnect(odbc::odbc(),
                 driver = "ODBC Driver 17 for SQL Server",
                 server = "AWS-PROD-SQL\\Sockeye",
                 database = "Elmer",
                 trusted_connection = "yes",
                 port = 1433)
}

elmer_connection <- elmer_connect()

output_dir<-file.path(getwd(), 'outputs')



if(!dir.exists(output_dir)){ 
  dir.create(output_dir)
}




network_file_loc<-'X:/DSA/Trends/equity/images'

# Create Image 
#image1 <- image_read(file.path(network_file_loc, 'plaza_roberto.png'))
image1 <- image_read(file.path(network_file_loc, "mighty_women.jpg"))

input <- c(image_scale(image1, "x200"))
women_image_header <- image_append(input)
image_write(women_image_header, path="women_image_header.png", format="png")
install_psrc_fonts()
women_image_header
```
![](women_image_header.png){width=100%}
\begin{flushleft}
wordswordswords
\end{flushleft}

## some header
\begin{flushleft}
more words
\end{flushleft}



```{r, echo=FALSE, results=TRUE,  fig.dim=c(6,4)}
wb <- createWorkbook()
xlsx_file <- "womens_history.xlsx"

```


```{r overallmodeshare, echo=FALSE, results=TRUE,  fig.dim=c(6,4), fig.dim=c(6,4) }
earnings_df<-read.csv('earnings.csv')
earnings_chart<- static_column_chart(t=earnings_df, y='estimate', x= 'name', fill="label", moe='moe', color='psrc_light', est='currency')
earnings_df

earnings_chart
```

```{r person variables}
source("womens_history_data.R")
```

\subsection{Conclusion}
\begin{flushleft}
someotherwords
\end{flushleft}

